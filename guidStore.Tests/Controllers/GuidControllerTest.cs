using guidStore.Controllers;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace guidStore.Tests.Controllers 
{
    [TestClass]
    public class GuidControllerTest 
    {
        [TestMethod]
        public void ShouldReturnBadRequestIfGuidIsNotSuppliedOnPut()
        {
            //todo: getting Newtonsoft errors here
            // Arrange
            //var controller = new GuidController();

            // Act
            //var result = controller.Put("", null);

            // Assert
            //Assert.IsType<BadRequest>(result);
        }
    }
}